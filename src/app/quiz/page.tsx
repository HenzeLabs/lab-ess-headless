import { redirect } from 'next/navigation';

// Quiz page is temporarily disabled for data verification
// To re-enable: restore the original page.tsx content from git history
export default function QuizPage() {
  redirect('/');
}
